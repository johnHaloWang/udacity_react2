import React from 'react'
import { connect } from 'react-redux'

function QuestionDetial({}) {
    return (
        <div>
            
        </div>
    )
}

export default connect()(QuestionDetial)
